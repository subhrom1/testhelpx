#
# helix configuration for local development
#
version: 1

definitions:
  defaults:
    - &defaultRepo https://github.com/subhrom1/testhelpx.git#master

strains:
  - name: default
    code: *defaultRepo
    content:  *defaultRepo
    static: https://github.com/subhrom1/testhelpx.git/htdocs#master

